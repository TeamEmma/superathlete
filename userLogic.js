// Check in with buddy
function onClick () {
  workout.buddy = true;
}

// //Select hero
// function onClick (hero_id) {
//   user.hero = Hero.find(hero_id);
// }

// Calculate user level
function calculateLevel(points) {
  return Math.floor(points/100);
}
